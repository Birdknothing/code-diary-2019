// process.on("message", fs.writeSync(1));
// const fs = require("fs");
// process.on("message", msg => {
//   console.log(msg);

//   fs.writeFileSync("./3.text", msg);
// });
setTimeout(() => {
  console.log("end");
}, 3000);
